{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node -e \"require('https').get(process.env.CRON_ENDPOINT || 'https://your-backend-url.railway.app/api/notifications/sms-alert?force=0', (r) => { r.on('data', () => {}); r.on('end', () => process.exit(0)); }).on('error', (e) => { console.error(e); process.exit(1); });\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
